import { NextResponse } from "next/server";
import { createSupabaseClient } from "@/lib/supabase/client";
export const runtime = "nodejs";
function splitText(text: string, size = 900) { const clean = text.replace(/\r/g, "").trim(); const chunks: string[] = []; for (let i = 0; i < clean.length; i += size) chunks.push(clean.slice(i, i + size).trim()); return chunks.filter(Boolean); }
export async function GET() { try { const supabase = createSupabaseClient(); const { data, error } = await supabase.from("knowledge_documents").select("id,title,file_name,description,status,created_at,updated_at,knowledge_chunks(count)").order("created_at", { ascending: false }); if (error) throw error; return NextResponse.json({ documents: data ?? [] }); } catch { return NextResponse.json({ error: "Unable to load knowledge documents." }, { status: 500 }); } }
export async function POST(request: Request) { try { const body = await request.json(); const title = typeof body.title === "string" ? body.title.trim() : ""; const text = typeof body.text === "string" ? body.text.trim() : ""; const description = typeof body.description === "string" ? body.description.trim() : ""; if (!title || !text) return NextResponse.json({ error: "Title and document text are required." }, { status: 400 }); const supabase = createSupabaseClient(); const { data: doc, error: docError } = await supabase.from("knowledge_documents").insert({ title, description, file_name: typeof body.fileName === "string" ? body.fileName.trim() : null, status: "processing" }).select("id,title,file_name,description,status,created_at,updated_at").single(); if (docError) throw docError; const chunks = splitText(text); const { error: chunkError } = await supabase.from("knowledge_chunks").insert(chunks.map((content, chunk_index) => ({ document_id: doc.id, content, chunk_index }))); if (chunkError) { await supabase.from("knowledge_documents").update({ status: "error" }).eq("id", doc.id); throw chunkError; } const { data: ready } = await supabase.from("knowledge_documents").update({ status: "ready" }).eq("id", doc.id).select("id,title,file_name,description,status,created_at,updated_at").single(); return NextResponse.json({ document: ready ?? doc, chunks: chunks.length }, { status: 201 }); } catch (error) { console.error("Knowledge ingestion error", error); return NextResponse.json({ error: "Unable to add the knowledge document." }, { status: 500 }); } }
